import React from "react";
import Bonus from "./Bonus";
import Account from "./Account";

const Reduxx = () => {
  return (
    <div>
      <h4>Redux</h4>
      <Bonus />
      <Account />
    </div>
  );
};

export default Reduxx;
